{
    "name": "sloth-pipe",
    "description": "A pipe utility that lazily evaluates only when needed, and only once.",
    "version": "0.0.2",
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsup pipe.ts fifo-queue.ts --format cjs,esm --dts --clean --sourcemap",
        "lint": "tsc"
    },
    "devDependencies": {
        "bun-types": "1.0.20",
        "mitata": "^0.1.6",
        "tsup": "^8.0.1",
        "typescript": "5.3.3"
    },
    "files": [
        "dist"
    ]
}
